import { describe, it, expect } from "vitest";

import { MapModel } from "./model.js";

describe("MapModel", function () {
  it("Should create an empty map", async function () {
    const m = new MapModel();
  });
  // it("Should create an empty map", async function() {});
});
